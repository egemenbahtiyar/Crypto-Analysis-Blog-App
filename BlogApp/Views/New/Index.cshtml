@model IEnumerable<BlogApp.Models.New>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HaberBaslik)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HaberEklenmeTarihi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HaberBaslik)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HaberEklenmeTarihi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NewId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NewId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NewId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
